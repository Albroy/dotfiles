- defaults:
      link:
              relink: true

- clean: ['~']

- link:
    ~/.bashrc: bash/bashrc
    ~/.vimrc: editors/vim/vimrc
    ~/.gitconfig: git/gitconfig

    ~/.tmux:  tmux
    ~/.tmux.conf: tmux/tmux.conf
